<%= form_with(model: character) do |form| %>
<% if character.errors.any? %>
<div style="color: red">
  <h2><%= pluralize(character.errors.count, "error") %> prohibited this character from being saved:</h2>

  <ul>
    <% character.errors.each do |error| %>
    <li><%= error.full_message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div>
  <%= form.label :first_name, style: "display: block", class: "form-label" %>
  <%= form.text_field :first_name, class: "form-control" %>
</div>

<div>
  <%= form.label :last_name, style: "display: block", class: "form-label" %>
  <%= form.text_field :last_name, class: "form-control" %>
</div>

<div>
  <%= form.label :origin, style: "display: block", class: "form-label" %>
  <%= form.text_field :origin, class: "form-control" %>
</div>

<div>
  <%= form.number_field :user_id, id: :character_user_id, class: "form-control", value: current_user.id, type: :hidden %>
</div>
<!-- o código abaixo exibirá o endereço na memória que armazena o usuário
  <%= current_user %> -->
<!-- inspecionar usuário
  <%= current_user.inspect %> -->
  <br>
  <div>
    <%= form.submit class: 'btn btn-primary' %>
  </div>
  <% end %>
